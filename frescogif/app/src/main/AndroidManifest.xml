<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frescogif">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashActivity"
            android:theme="@style/MyStyle">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:launchMode="singleTask">

        </activity>
        <activity android:name=".ImageBackgroundResilience">

        </activity>
        <activity android:name=".LoginActivity"
            android:theme="@style/MyStyle">

        </activity>
        <activity android:name=".activity.RecycleViewActivity">

        </activity>
        <activity android:name=".activity.SlidMenuActivity"
            android:theme="@style/AppSideTheme">

        </activity>
    </application>

</manifest>